class Product {
  final String name; // Stores products name
  final String imagePath; // Stores products name
  int quantity; // Stores products quantity chosen
  final double price; // Stores products price

  Product({this.price, this.name, this.imagePath, this.quantity});
}
